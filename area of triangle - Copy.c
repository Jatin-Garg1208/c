#include <stdio.h>
int main()
{
int height,base;
float area;
printf("Enter the height and base of right triangle :");
scanf("%d%d",&height,&base);
area = 1/2*height*base;
printf("Area of the right triangle is %f\n",area);
}
